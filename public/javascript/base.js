// Fonction retour page précédente au click sur le chevron
function retour() {
    window.history.back();
}

// Bloque l'orientation de l'écran en mode portrait
screen.orientation.lock("portrait-primary");